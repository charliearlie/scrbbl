version: 2.1
jobs:
  build:
    docker:
      - image: node:16.17.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: yarn install
      - run:
          name: Build
          command: yarn build
  lint:
    docker:
      - image: node:16.17.0
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: yarn install
      - run:
          name: Lint
          command: yarn lint

workflows:
  version: 2
  build-and-lint:
    jobs:
      - build
      - lint:
          requires:
            - build
          filters:
            branches:
              only: /.*/
